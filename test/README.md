# TEST RESLICE PERFORMANCES

- the function to be improved is getReslicedPixelData in image_utils
- for testing purpose, the whole module has been copied into test folder
- since the code will be modified only on a specific function, the unit test is delimited
- install node modules w/ yarn install before running the test
- run test w/ node testReslice.js input_file.json new_output.json
